<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<!-- 회원 -->
	<!-- 회원가입 -->
	<insert id="memberMapper">
		INSERT INTO MEMBER(MNAME MID, MPW) VALUES(#{mname},#{mid}, #{mpw}) 
	</insert>
	<!-- 회원정보 수정 -->
	<update id="updateMember">
		UPDATE MEMBER SET MNAME=#{mname} AND MPW = #{mpw} WHERE MID = #{mid}
	</update>
	<!-- 회원탈퇴 -->
	<delete id="deleteMember">
		DELETE FROM MEMBER WHERE MID= #{mid} AND MPW = #{mpw} AND MNAME=#{mname}
	</delete>
	
	<!-- member -->
	<!-- 로그인 -->
	<insert id="login" resultType="com.w.watching.DTO.MemberDTO">
		SELECT MID,MPW FROM MEMBER WHERE MID = #{mid} AND MPW = #{mpw}
	</insert>
	<!-- 패스워드 체크 -->
	<select id="passChk" resultType="int">
		SELECT COUNT(*) FROM MEMBER WHERE MID = #{mid} AND MPW = #{mpw}
	</select>
	<!-- 아이디 중복 체크 -->
	<select id="idChk" resultType="int">
		SELECT COUNT(*) FROM MEMBER WHERE MID=#{mid}
	</select>
	
</mapper>