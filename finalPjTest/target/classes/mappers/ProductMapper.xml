<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finalPj.testpj.productMapper">
	
	<!-- admin 화면 리스트 출력 -->
	<select id="list" resultType="com.finalPj.testpj.dto.ProductDTO">
		select * from product order by pCode desc
	</select>
	
	<!-- admin 상세페이지 여기서 수정버튼 누르면 수정되는걸로-->
	<select id="view" resultType="com.finalPj.testpj.dto.ProductDTO">
		select * from product where pcode=#{pCode}
	</select>
	
	<insert id="upload">
		insert into product (pCode, kCode, tCode, pName, aId, pUphit, pImg, pUploadDate) 
			values (product_SEQ.nextval, #{kCode}, #{tCode}, #{pName}, #{aId}, 0, #{pImg}, sysdate)	
	</insert>
	
	<update id="modify">
		update product set kCode=#{kCode}, tCode=#{tCode}, pName=#{pName}, aId=#{aId}
			where pCode=#{pCode}
	</update>
	
	<delete id="delete">
		delete from product where pCode=#{pCode}
	</delete>
	<!-- data table
		<insert id="dataUpload">
			insert into data (dName, dType, dUploadDate, pCode, kCode, tCode) 
				values (#{dName}, #{dType}, sysdate, #{pCode}, #{kCode}, #{tCode})	
		</insert>
		
		<delete id="dataDelete">
			delete from data where pCode=#{pCode}
		</delete>
		<select id="getData" resultType="com.finalPj.testpj.dto.ProductDTO">
			select * from data where pCode=#{pCode}
		</select>
	-->
	<select id="getPcode" resultType="int">
		select pCode from product where pName=#{pName}
	</select>	
	
	<update id="upHit">
		update product set pUphit = pUphit+1 where pCode=#{pCode}
	</update>
</mapper>